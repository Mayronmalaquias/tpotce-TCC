# Versoes fixas: precisam bater com backend/requirements.txt.
# O backend carrega o .joblib gerado aqui; versoes diferentes de scikit-learn
# entre treino e execucao geram InconsistentVersionWarning e nao garantem a
# mesma predicao. O bloco `environment` do *_meta.json registra o que foi usado.

scikit-learn==1.5.2
pandas==3.0.5
numpy==2.4.6
joblib==1.5.3

# opcional — so necessario se usar --model xgboost
# xgboost>=2.0.0
